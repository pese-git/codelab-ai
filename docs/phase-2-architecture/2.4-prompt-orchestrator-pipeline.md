# 2.4 — Разбивка `PromptOrchestrator` на Pipeline стадии

**Приоритет:** 🟠 Высокий  
**Оценка:** 2 дня  
**Файл:** `src/codelab/server/protocol/handlers/prompt_orchestrator.py`

---

## Проблема

`PromptOrchestrator` (~700 строк) нарушает Single Responsibility Principle. Конструктор принимает 7 зависимостей и класс отвечает за:
- Валидацию входных данных
- Обработку slash-команд
- Построение планов
- Управление жизненным циклом turn
- Вызов LLM-агента
- Обработку tool calls
- Управление разрешениями
- Client RPC запросы

При добавлении нового типа инструмента или нового шага обработки — неизбежно модифицируется центральный класс.

---

## Решение: Pipeline Pattern

Ввести абстракцию `PromptStage` — каждая стадия получает контекст, выполняет свою работу, возвращает изменённый контекст или останавливает pipeline.

### Шаг 1 — Создать модели контекста и результата

Создать `src/codelab/server/protocol/handlers/pipeline/context.py`:

```python
"""Контекст выполнения pipeline обработки prompt-turn."""

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from ...messages import ACPMessage, JsonRpcId
from ...protocol.state import SessionState


@dataclass
class PromptContext:
    """Изменяемый контекст, передаваемый через все стадии pipeline."""

    # Входные данные
    session_id: str
    session: SessionState
    request_id: JsonRpcId
    params: dict[str, Any]
    raw_text: str

    # Результаты, накапливаемые по ходу pipeline
    notifications: list[ACPMessage] = field(default_factory=list)
    stop_reason: str = "end_turn"
    should_stop: bool = False       # True — прервать pipeline досрочно
    error_response: ACPMessage | None = None

    # Метаданные для передачи между стадиями
    meta: dict[str, Any] = field(default_factory=dict)
```

### Шаг 2 — Создать абстракцию стадии

Создать `src/codelab/server/protocol/handlers/pipeline/base.py`:

```python
"""Базовая абстракция стадии pipeline."""

from __future__ import annotations

from abc import ABC, abstractmethod

from .context import PromptContext


class PromptStage(ABC):
    """Одна стадия обработки prompt-turn.

    Каждая стадия получает контекст, выполняет работу и:
    - возвращает context (продолжить pipeline)
    - устанавливает context.should_stop = True (прервать, успешно)
    - устанавливает context.error_response (прервать с ошибкой)
    """

    @property
    def name(self) -> str:
        return self.__class__.__name__

    @abstractmethod
    async def process(self, context: PromptContext) -> PromptContext:
        """Выполнить стадию. Изменить context и вернуть его."""
        ...
```

### Шаг 3 — Реализовать стадии

Создать `src/codelab/server/protocol/handlers/pipeline/stages/`:

```python
# stages/validation.py
class ValidationStage(PromptStage):
    """Валидация входных параметров и проверка состояния сессии."""

    def __init__(self, state_manager: StateManager) -> None:
        self._state_manager = state_manager

    async def process(self, context: PromptContext) -> PromptContext:
        # Проверить что сессия не занята
        if context.session.active_turn is not None:
            context.error_response = ACPMessage.error_response(
                context.request_id, code=-32003, message="Session busy"
            )
            context.should_stop = True
            return context

        # Проверить текст промпта
        if not context.raw_text.strip():
            context.error_response = ACPMessage.error_response(
                context.request_id, code=-32602, message="Empty prompt"
            )
            context.should_stop = True
            return context

        return context
```

```python
# stages/slash_commands.py
class SlashCommandStage(PromptStage):
    """Обработка slash-команд (/help, /mode, /status и т.д.)."""

    def __init__(self, slash_router: SlashCommandRouter) -> None:
        self._router = slash_router

    async def process(self, context: PromptContext) -> PromptContext:
        if not context.raw_text.startswith("/"):
            return context

        result = await self._router.handle(context.raw_text, context.session)
        if result is not None:
            context.notifications.extend(result.notifications)
            context.should_stop = True  # slash-команда обработана, LLM не нужен
        return context
```

```python
# stages/plan_building.py
class PlanBuildingStage(PromptStage):
    """Построение плана выполнения задачи."""

    def __init__(self, plan_builder: PlanBuilder) -> None:
        self._plan_builder = plan_builder

    async def process(self, context: PromptContext) -> PromptContext:
        plan_notifications = await self._plan_builder.build(
            context.session, context.raw_text
        )
        context.notifications.extend(plan_notifications)
        return context
```

```python
# stages/llm_loop.py
class LLMLoopStage(PromptStage):
    """Основной цикл взаимодействия с LLM и выполнения tool calls."""

    def __init__(
        self,
        agent_orchestrator: AgentOrchestrator,
        tool_call_handler: ToolCallHandler,
        permission_manager: PermissionManager,
    ) -> None:
        self._agent = agent_orchestrator
        self._tool_handler = tool_call_handler
        self._permission = permission_manager

    async def process(self, context: PromptContext) -> PromptContext:
        result = await self._agent.process_prompt(
            session=context.session,
            text=context.raw_text,
        )
        context.notifications.extend(result.notifications)
        context.stop_reason = result.stop_reason
        return context
```

```python
# stages/turn_lifecycle.py
class TurnLifecycleStage(PromptStage):
    """Управление началом и завершением turn, обновление events_history."""

    def __init__(self, turn_manager: TurnLifecycleManager) -> None:
        self._turn_manager = turn_manager

    async def process(self, context: PromptContext) -> PromptContext:
        close_notifications = await self._turn_manager.close_turn(
            context.session, context.stop_reason
        )
        context.notifications.extend(close_notifications)
        return context
```

### Шаг 4 — Собрать Pipeline

Создать `src/codelab/server/protocol/handlers/pipeline/runner.py`:

```python
"""Pipeline runner — запускает стадии последовательно."""

from __future__ import annotations

import structlog

from .base import PromptStage
from .context import PromptContext

logger = structlog.get_logger()


class PromptPipeline:
    """Исполнитель pipeline стадий обработки prompt-turn."""

    def __init__(self, stages: list[PromptStage]) -> None:
        self._stages = stages

    async def run(self, context: PromptContext) -> PromptContext:
        for stage in self._stages:
            logger.debug("pipeline_stage_start", stage=stage.name)
            try:
                context = await stage.process(context)
            except Exception as e:
                logger.error("pipeline_stage_error", stage=stage.name, error=str(e))
                context.error_response = ACPMessage.error_response(
                    context.request_id, code=-32603, message=f"Internal error in {stage.name}"
                )
                context.should_stop = True

            if context.should_stop:
                logger.debug("pipeline_stopped", stage=stage.name, has_error=context.error_response is not None)
                break

        return context
```

### Шаг 5 — Создать фабрику pipeline в `PromptOrchestrator`

```python
# prompt_orchestrator.py — финальная версия:
class PromptOrchestrator:
    """Фабрика и точка входа для обработки prompt-turn через Pipeline."""

    def __init__(
        self,
        state_manager: StateManager,
        plan_builder: PlanBuilder,
        turn_lifecycle_manager: TurnLifecycleManager,
        tool_call_handler: ToolCallHandler,
        permission_manager: PermissionManager,
        client_rpc_handler: ClientRPCHandler,
        tool_registry: ToolRegistry,
    ) -> None:
        self._pipeline = PromptPipeline(stages=[
            ValidationStage(state_manager),
            SlashCommandStage(self._build_slash_router()),
            PlanBuildingStage(plan_builder),
            TurnLifecycleStage(turn_lifecycle_manager),   # open turn
            LLMLoopStage(tool_call_handler, permission_manager),
            TurnLifecycleStage(turn_lifecycle_manager),   # close turn
        ])

    async def process_prompt_turn(
        self,
        session: SessionState,
        session_id: str,
        request_id: JsonRpcId,
        params: dict[str, Any],
        agent_orchestrator: AgentOrchestrator,
    ) -> LLMLoopResult:
        context = PromptContext(
            session_id=session_id,
            session=session,
            request_id=request_id,
            params=params,
            raw_text=params.get("text", ""),
        )
        result = await self._pipeline.run(context)
        return LLMLoopResult(
            notifications=result.notifications,
            stop_reason=result.stop_reason,
        )
```

---

## Тесты

```python
@pytest.mark.asyncio
async def test_pipeline_stops_on_validation_error():
    state_manager = MagicMock()
    stage = ValidationStage(state_manager)

    # Активный turn — должен заблокировать
    session = SessionState(session_id="s1", cwd="/", mcp_servers=[])
    session.active_turn = ActiveTurnState(prompt_request_id="req-0", session_id="s1")

    context = PromptContext(session_id="s1", session=session, request_id="req-1",
                             params={}, raw_text="hello")
    result = await stage.process(context)

    assert result.should_stop is True
    assert result.error_response is not None


@pytest.mark.asyncio
async def test_slash_command_stage_stops_pipeline():
    router = MagicMock()
    router.handle = AsyncMock(return_value=SlashCommandResult(notifications=[]))
    stage = SlashCommandStage(router)

    context = PromptContext(session_id="s1", session=MagicMock(),
                             request_id="req-1", params={}, raw_text="/help")
    result = await stage.process(context)

    assert result.should_stop is True
    router.handle.assert_awaited_once()


@pytest.mark.asyncio
async def test_pipeline_runs_all_stages():
    stage1 = MagicMock(spec=PromptStage)
    stage1.process = AsyncMock(side_effect=lambda ctx: ctx)
    stage2 = MagicMock(spec=PromptStage)
    stage2.process = AsyncMock(side_effect=lambda ctx: ctx)

    pipeline = PromptPipeline(stages=[stage1, stage2])
    context = PromptContext(session_id="s1", session=MagicMock(),
                             request_id="req-1", params={}, raw_text="hello")
    await pipeline.run(context)

    stage1.process.assert_awaited_once()
    stage2.process.assert_awaited_once()
```
